# PostgreSQL Database Configuration
# Render.com PostgreSQL Database (External URL for local development)
# ⚠️ 실제 DATABASE_URL을 Render.com 대시보드에서 복사해서 붙여넣으세요
DATABASE_URL=postgresql://ahp_postgresql_user:your_password@dpg-d2q8l5qdbo4c73bt3780-a.oregon-postgres.render.com/ahp_postgresql

# Local development database (commented out for testing without PostgreSQL)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/ahp_db

# JWT Configuration
JWT_SECRET=ahp-secret-key-2024-development
JWT_REFRESH_SECRET=ahp-refresh-secret-key-2024-development
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Security
BCRYPT_ROUNDS=10

# Session Configuration  
SESSION_SECRET=ahp-session-secret-2024-development

# Application Configuration
APP_NAME=AHP Decision System
APP_VERSION=2.2.0

# For production on Render.com, use:
# DATABASE_URL=(Render.com PostgreSQL internal URL - 자동으로 설정됨)
# NODE_ENV=production
# CORS_ORIGIN=https://aebonlee.github.io/ahp_app