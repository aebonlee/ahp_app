# AHP 의사결정 지원 플랫폼 - 상세 기술 명세서

## 1. 시스템 아키텍처

### 1.1 전체 시스템 구성도

```
┌─────────────────────────────────────────────────────────────┐
│                        사용자 인터페이스 계층                   │
├─────────────────────────────────────────────────────────────┤
│  React 18.2  │  TypeScript 4.9  │  TailwindCSS 3.x          │
│  WebSocket   │  PWA             │  Responsive Design        │
└─────────────────────────────────────────────────────────────┘
                                ↕
┌─────────────────────────────────────────────────────────────┐
│                      API Gateway (REST/WebSocket)             │
├─────────────────────────────────────────────────────────────┤
│  Django REST Framework 3.x    │  JWT Authentication          │
│  CORS Handler                 │  Rate Limiting               │
└─────────────────────────────────────────────────────────────┘
                                ↕
┌─────────────────────────────────────────────────────────────┐
│                        비즈니스 로직 계층                      │
├─────────────────────────────────────────────────────────────┤
│  AHP 계산 엔진  │  일관성 검증기  │  통합 분석기             │
│  AI 서비스      │  실시간 동기화   │  민감도 분석             │
└─────────────────────────────────────────────────────────────┘
                                ↕
┌─────────────────────────────────────────────────────────────┐
│                         데이터 계층                           │
├─────────────────────────────────────────────────────────────┤
│  PostgreSQL 17.0  │  Redis Cache  │  Object Storage          │
│  Vector DB        │  Message Queue │  Backup System          │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 마이크로서비스 아키텍처

```
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  프로젝트    │  │   평가자     │  │   분석       │
│  관리 서비스 │  │  관리 서비스 │  │   서비스     │
└──────────────┘  └──────────────┘  └──────────────┘
       ↓                 ↓                 ↓
┌──────────────────────────────────────────────────┐
│             메시지 브로커 (RabbitMQ)              │
└──────────────────────────────────────────────────┘
       ↓                 ↓                 ↓
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│   AI 통합    │  │  알림/이메일 │  │   보고서     │
│   서비스     │  │    서비스    │  │   생성 서비스 │
└──────────────┘  └──────────────┘  └──────────────┘
```

---

## 2. 핵심 알고리즘 상세

### 2.1 AHP 가중치 계산 알고리즘

#### 2.1.1 고유벡터 방법 (Eigenvector Method)

```python
def calculate_priority_vector(comparison_matrix):
    """
    쌍대비교 행렬에서 우선순위 벡터 계산
    특허 핵심: 실시간 점진적 계산 최적화
    """
    n = len(comparison_matrix)
    
    # 1단계: 행렬 정규화
    normalized_matrix = normalize_columns(comparison_matrix)
    
    # 2단계: 행 평균 계산
    priority_vector = calculate_row_means(normalized_matrix)
    
    # 3단계: 일관성 검증
    consistency_ratio = calculate_consistency_ratio(
        comparison_matrix, priority_vector
    )
    
    # 4단계: 자동 보정 (특허 핵심 기술)
    if consistency_ratio > 0.1:
        priority_vector = auto_adjust_priorities(
            comparison_matrix, priority_vector, consistency_ratio
        )
    
    return priority_vector, consistency_ratio
```

#### 2.1.2 일관성 비율(CR) 계산

```python
def calculate_consistency_ratio(matrix, priority_vector):
    """
    일관성 비율 실시간 계산
    특허 핵심: 동적 임계값 조정
    """
    n = len(matrix)
    
    # λmax 계산
    weighted_sum = matrix @ priority_vector
    lambda_max = np.mean(weighted_sum / priority_vector)
    
    # 일관성 지수(CI) 계산
    ci = (lambda_max - n) / (n - 1)
    
    # 무작위 일관성 지수(RI)
    ri_values = {3: 0.58, 4: 0.90, 5: 1.12, 6: 1.24, 
                 7: 1.32, 8: 1.41, 9: 1.45, 10: 1.49}
    ri = ri_values.get(n, 1.49)
    
    # 일관성 비율(CR)
    cr = ci / ri if ri > 0 else 0
    
    return cr
```

### 2.2 그룹 의사결정 통합 알고리즘

#### 2.2.1 가중 기하평균 방법 (WGMM)

```python
def aggregate_group_judgments(individual_matrices, evaluator_weights):
    """
    다수 평가자의 판단 통합
    특허 핵심: 동적 가중치 조정 및 이상치 감지
    """
    n = len(individual_matrices[0])
    m = len(individual_matrices)
    
    # 통합 행렬 초기화
    aggregated_matrix = np.ones((n, n))
    
    for i in range(n):
        for j in range(n):
            if i != j:
                # 가중 기하평균 계산
                product = 1.0
                for k, matrix in enumerate(individual_matrices):
                    weight = evaluator_weights[k]
                    product *= matrix[i][j] ** weight
                
                aggregated_matrix[i][j] = product
                aggregated_matrix[j][i] = 1.0 / product
    
    # 이상치 감지 및 조정
    aggregated_matrix = detect_and_adjust_outliers(aggregated_matrix)
    
    return aggregated_matrix
```

#### 2.2.2 평가자 신뢰도 기반 가중치 계산

```python
def calculate_evaluator_weights(consistency_ratios, completion_rates):
    """
    평가자별 동적 가중치 계산
    특허 핵심: 다차원 신뢰도 평가
    """
    weights = []
    
    for cr, completion in zip(consistency_ratios, completion_rates):
        # 일관성 기반 점수
        consistency_score = max(0, 1 - cr / 0.1)
        
        # 완성도 기반 점수
        completion_score = completion
        
        # 통합 가중치
        weight = 0.7 * consistency_score + 0.3 * completion_score
        weights.append(weight)
    
    # 정규화
    total_weight = sum(weights)
    normalized_weights = [w / total_weight for w in weights]
    
    return normalized_weights
```

### 2.3 AI 기반 평가 지원 알고리즘

#### 2.3.1 판단 보조 시스템

```python
class AIJudgmentAssistant:
    """
    AI 기반 쌍대비교 판단 지원
    특허 핵심: 컨텍스트 인식 추천
    """
    
    def suggest_comparison_value(self, criterion_a, criterion_b, context):
        """
        쌍대비교 값 추천
        """
        # 1. 의미론적 유사도 분석
        semantic_similarity = self.calculate_semantic_similarity(
            criterion_a, criterion_b
        )
        
        # 2. 과거 데이터 기반 패턴 분석
        historical_pattern = self.analyze_historical_patterns(
            criterion_a, criterion_b, context
        )
        
        # 3. 도메인 지식 적용
        domain_knowledge = self.apply_domain_knowledge(
            criterion_a, criterion_b, context
        )
        
        # 4. 통합 추천값 계산
        suggested_value = self.integrate_recommendations(
            semantic_similarity,
            historical_pattern,
            domain_knowledge
        )
        
        # 5. 신뢰도 점수 계산
        confidence = self.calculate_confidence_score(
            semantic_similarity,
            historical_pattern,
            domain_knowledge
        )
        
        return {
            'value': suggested_value,
            'confidence': confidence,
            'reasoning': self.generate_explanation(suggested_value)
        }
```

#### 2.3.2 자동 보고서 생성

```python
class AIReportGenerator:
    """
    AI 기반 분석 보고서 자동 생성
    특허 핵심: 맞춤형 인사이트 생성
    """
    
    def generate_report(self, analysis_results):
        """
        종합 분석 보고서 생성
        """
        report_sections = []
        
        # 1. 핵심 발견사항
        key_findings = self.extract_key_findings(analysis_results)
        report_sections.append(self.format_key_findings(key_findings))
        
        # 2. 상세 분석
        detailed_analysis = self.perform_detailed_analysis(analysis_results)
        report_sections.append(self.format_detailed_analysis(detailed_analysis))
        
        # 3. 시각화 생성
        visualizations = self.generate_visualizations(analysis_results)
        report_sections.append(visualizations)
        
        # 4. 권고사항
        recommendations = self.generate_recommendations(analysis_results)
        report_sections.append(self.format_recommendations(recommendations))
        
        # 5. 리스크 분석
        risk_analysis = self.analyze_risks(analysis_results)
        report_sections.append(self.format_risk_analysis(risk_analysis))
        
        return self.compile_report(report_sections)
```

---

## 3. 실시간 협업 메커니즘

### 3.1 WebSocket 기반 실시간 동기화

```javascript
class RealTimeCollaborationEngine {
    constructor() {
        this.websocket = new WebSocket('wss://api.ahp-platform.com/ws');
        this.syncQueue = [];
        this.conflictResolver = new ConflictResolver();
    }
    
    // 실시간 데이터 동기화
    synchronizeData(data) {
        // 1. 로컬 변경사항 감지
        const changes = this.detectLocalChanges(data);
        
        // 2. 변경사항 브로드캐스트
        this.broadcastChanges(changes);
        
        // 3. 충돌 감지 및 해결
        const conflicts = this.detectConflicts(changes);
        if (conflicts.length > 0) {
            const resolved = this.conflictResolver.resolve(conflicts);
            this.applyResolvedChanges(resolved);
        }
        
        // 4. 모든 클라이언트 동기화
        this.syncAllClients(changes);
    }
    
    // Operational Transform 알고리즘
    transformOperation(op1, op2) {
        // 특허 핵심: 동시 편집 충돌 해결
        if (op1.type === 'insert' && op2.type === 'insert') {
            if (op1.position < op2.position) {
                return [op1, {...op2, position: op2.position + op1.length}];
            } else {
                return [{...op1, position: op1.position + op2.length}, op2];
            }
        }
        // ... 추가 변환 로직
    }
}
```

### 3.2 분산 잠금 메커니즘

```python
class DistributedLockManager:
    """
    분산 환경에서의 데이터 일관성 보장
    특허 핵심: 적응적 잠금 전략
    """
    
    def acquire_lock(self, resource_id, user_id, timeout=5):
        """
        리소스 잠금 획득
        """
        lock_key = f"lock:{resource_id}"
        lock_value = f"{user_id}:{time.time()}"
        
        # Redis를 사용한 분산 잠금
        acquired = self.redis_client.set(
            lock_key, 
            lock_value, 
            nx=True, 
            ex=timeout
        )
        
        if not acquired:
            # 잠금 대기 큐에 추가
            self.add_to_wait_queue(resource_id, user_id)
            
            # 적응적 대기 전략
            wait_time = self.calculate_adaptive_wait_time(resource_id)
            time.sleep(wait_time)
            
            # 재시도
            return self.acquire_lock(resource_id, user_id, timeout)
        
        return True
```

---

## 4. 민감도 분석 엔진

### 4.1 파라미터 민감도 분석

```python
class SensitivityAnalysisEngine:
    """
    의사결정 결과의 민감도 분석
    특허 핵심: 다차원 민감도 매트릭스
    """
    
    def perform_sensitivity_analysis(self, base_weights, alternatives_scores):
        """
        종합 민감도 분석 수행
        """
        sensitivity_results = {}
        
        # 1. 단일 기준 민감도
        for criterion_idx in range(len(base_weights)):
            sensitivity_results[f'criterion_{criterion_idx}'] = \
                self.single_criterion_sensitivity(
                    base_weights, 
                    alternatives_scores, 
                    criterion_idx
                )
        
        # 2. 교차 민감도 (2개 기준 동시 변화)
        cross_sensitivity = self.cross_criteria_sensitivity(
            base_weights, 
            alternatives_scores
        )
        sensitivity_results['cross_sensitivity'] = cross_sensitivity
        
        # 3. 임계값 분석
        threshold_analysis = self.threshold_analysis(
            base_weights, 
            alternatives_scores
        )
        sensitivity_results['thresholds'] = threshold_analysis
        
        # 4. 로버스트니스 지수 계산
        robustness_index = self.calculate_robustness_index(
            sensitivity_results
        )
        sensitivity_results['robustness'] = robustness_index
        
        return sensitivity_results
    
    def single_criterion_sensitivity(self, weights, scores, criterion_idx):
        """
        단일 기준 가중치 변화에 따른 민감도
        """
        sensitivity_curve = []
        original_weight = weights[criterion_idx]
        
        # 가중치를 0%에서 100%까지 변화
        for new_weight in np.linspace(0, 1, 101):
            # 다른 가중치 재조정
            adjusted_weights = self.adjust_other_weights(
                weights, 
                criterion_idx, 
                new_weight
            )
            
            # 새로운 순위 계산
            new_rankings = self.calculate_rankings(
                adjusted_weights, 
                scores
            )
            
            # 순위 변화 추적
            rank_changes = self.track_rank_changes(
                original_rankings, 
                new_rankings
            )
            
            sensitivity_curve.append({
                'weight': new_weight,
                'rankings': new_rankings,
                'changes': rank_changes
            })
        
        return sensitivity_curve
```

### 4.2 몬테카를로 시뮬레이션

```python
class MonteCarloSimulation:
    """
    불확실성 하에서의 의사결정 분석
    특허 핵심: 적응적 샘플링 전략
    """
    
    def run_simulation(self, base_judgments, uncertainty_levels, n_iterations=10000):
        """
        몬테카를로 시뮬레이션 실행
        """
        simulation_results = []
        
        for i in range(n_iterations):
            # 1. 불확실성을 반영한 판단 값 생성
            perturbed_judgments = self.perturb_judgments(
                base_judgments, 
                uncertainty_levels
            )
            
            # 2. AHP 계산 수행
            weights = self.calculate_ahp_weights(perturbed_judgments)
            
            # 3. 대안 평가
            alternative_scores = self.evaluate_alternatives(weights)
            
            # 4. 결과 저장
            simulation_results.append({
                'iteration': i,
                'judgments': perturbed_judgments,
                'weights': weights,
                'scores': alternative_scores,
                'ranking': self.get_ranking(alternative_scores)
            })
        
        # 5. 통계 분석
        statistics = self.analyze_simulation_results(simulation_results)
        
        return {
            'results': simulation_results,
            'statistics': statistics,
            'confidence_intervals': self.calculate_confidence_intervals(simulation_results),
            'probability_distributions': self.fit_distributions(simulation_results)
        }
```

---

## 5. 보안 및 프라이버시

### 5.1 익명 평가 시스템

```python
class AnonymousEvaluationSystem:
    """
    평가자 익명성 보장 시스템
    특허 핵심: 영지식 증명 기반 인증
    """
    
    def create_anonymous_session(self, evaluator_email):
        """
        익명 평가 세션 생성
        """
        # 1. 일회용 토큰 생성
        anonymous_token = self.generate_anonymous_token()
        
        # 2. 평가자 ID 해싱
        evaluator_hash = self.hash_evaluator_identity(evaluator_email)
        
        # 3. 영지식 증명 생성
        zk_proof = self.generate_zero_knowledge_proof(evaluator_hash)
        
        # 4. 세션 매핑 (단방향)
        session_mapping = {
            'token': anonymous_token,
            'proof': zk_proof,
            'expires': time.time() + 86400  # 24시간
        }
        
        # 5. 안전한 저장
        self.secure_storage.store(anonymous_token, session_mapping)
        
        return anonymous_token
    
    def verify_anonymous_evaluator(self, token, proof):
        """
        익명 평가자 검증
        """
        # 영지식 증명 검증
        is_valid = self.verify_zero_knowledge_proof(proof)
        
        if is_valid:
            # 평가 권한 부여 (신원 노출 없이)
            return self.grant_evaluation_permission(token)
        
        return False
```

### 5.2 데이터 암호화

```python
class DataEncryptionService:
    """
    엔드투엔드 데이터 암호화
    특허 핵심: 계층적 키 관리
    """
    
    def encrypt_sensitive_data(self, data, access_level):
        """
        접근 수준별 데이터 암호화
        """
        # 1. 데이터 분류
        classified_data = self.classify_data_sensitivity(data)
        
        # 2. 계층적 암호화
        encrypted_layers = {}
        for level, content in classified_data.items():
            # 레벨별 다른 키 사용
            encryption_key = self.get_encryption_key(level)
            
            # AES-256 암호화
            encrypted_content = self.aes_encrypt(content, encryption_key)
            
            encrypted_layers[level] = encrypted_content
        
        # 3. 메타데이터 추가
        encrypted_package = {
            'layers': encrypted_layers,
            'access_requirements': self.define_access_requirements(access_level),
            'timestamp': time.time(),
            'version': '1.0'
        }
        
        return encrypted_package
```

---

## 6. 성능 최적화 기술

### 6.1 캐싱 전략

```python
class IntelligentCachingSystem:
    """
    지능형 캐싱 시스템
    특허 핵심: 예측적 캐시 관리
    """
    
    def __init__(self):
        self.cache_layers = {
            'l1': {},  # 메모리 캐시
            'l2': RedisCache(),  # Redis 캐시
            'l3': CDNCache()  # CDN 캐시
        }
        self.ml_predictor = CachePredictionModel()
    
    def get_with_smart_cache(self, key):
        """
        다층 캐시에서 데이터 조회
        """
        # 1. L1 캐시 확인
        if key in self.cache_layers['l1']:
            self.update_access_pattern(key, 'l1')
            return self.cache_layers['l1'][key]
        
        # 2. L2 캐시 확인
        l2_result = self.cache_layers['l2'].get(key)
        if l2_result:
            # L1으로 승급
            self.promote_to_l1(key, l2_result)
            return l2_result
        
        # 3. L3 캐시 확인
        l3_result = self.cache_layers['l3'].get(key)
        if l3_result:
            # 예측 기반 승급 결정
            if self.ml_predictor.should_promote(key):
                self.promote_to_l2(key, l3_result)
            return l3_result
        
        # 4. 원본 데이터 조회 및 캐싱
        data = self.fetch_original_data(key)
        self.intelligent_cache_placement(key, data)
        
        return data
```

### 6.2 병렬 처리 최적화

```python
class ParallelProcessingOptimizer:
    """
    대규모 계산 병렬 처리
    특허 핵심: 동적 작업 분할
    """
    
    async def parallel_ahp_calculation(self, project_data):
        """
        병렬 AHP 계산 수행
        """
        # 1. 작업 분할 전략 결정
        partition_strategy = self.determine_partition_strategy(project_data)
        
        # 2. 작업 큐 생성
        task_queue = self.create_task_queue(project_data, partition_strategy)
        
        # 3. 워커 풀 할당
        worker_pool = self.allocate_workers(len(task_queue))
        
        # 4. 병렬 실행
        results = await asyncio.gather(*[
            self.process_task(task, worker) 
            for task, worker in zip(task_queue, worker_pool)
        ])
        
        # 5. 결과 병합
        merged_results = self.merge_parallel_results(results)
        
        # 6. 일관성 검증
        self.verify_consistency(merged_results)
        
        return merged_results
```

---

## 7. 확장성 및 통합

### 7.1 API 설계

```yaml
# OpenAPI 3.0 명세
openapi: 3.0.0
info:
  title: AHP Platform API
  version: 1.0.0

paths:
  /api/v1/projects:
    post:
      summary: 새 프로젝트 생성
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                title: 
                  type: string
                description: 
                  type: string
                evaluation_mode:
                  type: string
                  enum: [simple, advanced, fuzzy]
      responses:
        201:
          description: 프로젝트 생성 성공
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Project'
                
  /api/v1/evaluations/realtime:
    get:
      summary: 실시간 평가 스트림
      responses:
        200:
          description: SSE 스트림
          content:
            text/event-stream:
              schema:
                type: string
```

### 7.2 플러그인 아키텍처

```python
class PluginArchitecture:
    """
    확장 가능한 플러그인 시스템
    특허 핵심: 동적 플러그인 로딩
    """
    
    def __init__(self):
        self.plugins = {}
        self.hooks = defaultdict(list)
    
    def register_plugin(self, plugin_config):
        """
        플러그인 동적 등록
        """
        # 1. 플러그인 검증
        if not self.validate_plugin(plugin_config):
            raise InvalidPluginError()
        
        # 2. 의존성 확인
        self.check_dependencies(plugin_config)
        
        # 3. 플러그인 로드
        plugin_instance = self.load_plugin(plugin_config)
        
        # 4. 훅 등록
        for hook_name, handler in plugin_instance.get_hooks().items():
            self.hooks[hook_name].append(handler)
        
        # 5. 플러그인 활성화
        self.plugins[plugin_config['id']] = plugin_instance
        plugin_instance.activate()
        
        return plugin_instance
```

---

## 8. 기술 로드맵

### 8.1 현재 구현 완료 (v1.0)
- ✅ 기본 AHP 계산 엔진
- ✅ 실시간 협업 기능
- ✅ 일관성 검증 시스템
- ✅ 기본 민감도 분석
- ✅ 익명 평가 시스템

### 8.2 개발 중 (v2.0)
- 🚧 AI 기반 평가 가이드
- 🚧 고급 시각화
- 🚧 몬테카를로 시뮬레이션
- 🚧 블록체인 기반 감사 추적

### 8.3 계획 중 (v3.0)
- 📋 양자 컴퓨팅 최적화
- 📋 AR/VR 인터페이스
- 📋 IoT 디바이스 통합
- 📋 연합 학습 기반 개인화

---

**작성일**: 2025년 10월 24일  
**문서 버전**: 1.0  
**기밀 등급**: 대외비